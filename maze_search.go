package algo

/*
type mazeVertex struct {
	x, y  int
	edges []mazeEdge
}

type mazeEdge struct {
	from, to *mazeVertex
}

func constructMaze(node *mazeVertex, maze [][]bool) {

}

// MazeSearch returns the path required to traverse a maze starting at
// (startX, startY) and ending at (endX, endY)
func MazeSearch(maze [][]bool, startX, startY, endX, endY int) [][]int {

	root := &mazeVertex{
		x: startX,
		y: startY,
	}
}
*/
